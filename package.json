{
  "name": "secure-note-workspace",
  "version": "1.0.0",
  "description": "SecureNote - Complete End-to-End Encrypted Web Note Taking System",
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build": "npm run build:backend && npm run build:frontend",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "db:push": "npm --prefix backend run prisma:push",
    "db:generate": "npm --prefix backend run prisma:generate"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
