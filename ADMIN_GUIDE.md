# คู่มือการใช้งานและดูแลระบบ Admin Dashboard (NoteAll / Note on Web)

> **เอกสารคู่มือทางการสำหรับผู้ดูแลระบบ (Administrator Manual)**  
> เวอร์ชันระบบ: 1.0.0  
> สถาปัตยกรรม: Zero-Knowledge Privacy Architecture + End-to-End Encryption (E2EE)

---

## 1. ภาพรวมของระบบ (System Overview)

ระบบ **Admin Dashboard** ของ NoteAll ถูกสร้างขึ้นเพื่อเป็นศูนย์กลางการบริหารจัดการ ตรวจสอบความปลอดภัย ดูแลฐานข้อมูล และติดตามสถิติการใช้งานของแอปพลิเคชัน โดยยึดหลักการสูงสุด:

> **"Zero-Knowledge & Privacy First"**  
> ผู้ดูแลระบบทุกระดับ **ไม่สามารถเห็นรหัสผ่านจริง** และ **ไม่สามารถอ่านเนื้อหาโน้ตในห้องนิรภัย** ของผู้ใช้งานได้เด็ดขาด

---

## 2. โครงสร้างระดับสิทธิ์ของผู้ใช้งาน (User Roles & Permissions)

ระบบแบ่งผู้ใช้ออกเป็น **3 ระดับสิทธิ์** อย่างชัดเจน:

| ระดับสิทธิ์ (Role) | สัญลักษณ์ | คำอธิบายและขอบเขตอำนาจ |
| :--- | :---: | :--- |
| **`SUPER_ADMIN`** | 👑 | **ผู้ดูแลระบบสูงสุด (เจ้าของระบบตัวจริง):** มีอำนาจสูงสุดในการจัดการผู้ใช้, เปลี่ยนระดับสิทธิ์ของผู้อื่น, กู้คืนฐานข้อมูล (Restore), สำรองข้อมูล, ดาวน์โหลดไฟล์สำรอง และส่งออกประวัติกิจกรรม |
| **`ADMIN`** | 🛡️ | **ผู้ดูแลระบบทั่วไป:** ดูสถิติภาพรวม, ดูรายชื่อผู้ใช้, ตรวจสอบความจุกระดาน 56 แผ่น, ตรวจสอบความสมบูรณ์ DB, สั่งสำรองข้อมูล และดาวน์โหลดไฟล์สำรอง *(ไม่สามารถเปลี่ยนสิทธิ์คนอื่น และไม่สามารถกู้คืนฐานข้อมูลได้)* |
| **`USER`** | 👤 | **ผู้ใช้งานทั่วไป:** จดโน้ต, จัดการบอร์ด, ใช้ห้องนิรภัยส่วนตัว *(ไม่เห็นเมนูแอดมิน และถูกบล็อกไม่ให้เข้าถึง URL หรือ API ฝั่ง Admin 100%)* |

---

## 3. บัญชีผู้ดูแลระบบปัจจุบัน

1. **`heros5510@gmail.com`** ➔ **`SUPER_ADMIN`** *(บัญชีผู้ดูแลระบบสูงสุดตัวจริง)*
2. **`knowman@securenote.test`** ➔ **`SUPER_ADMIN`** *(บัญชีทดสอบระบบ รหัสผ่าน: `SystemTest`)*
3. **`aporngold@gmail.com`** ➔ **`ADMIN`** *(บัญชีผู้ดูแลระบบทั่วไป)*
4. **ผู้ใช้ท่านอื่นทั้งหมด และผู้สมัครใหม่ในอนาคต** ➔ ได้รับสิทธิ์ **`USER` ธรรมดาอัตโนมัติ**

---

## 4. วิธีเข้าใช้งาน Admin Dashboard

### บนเครื่องคอมพิวเตอร์ (Desktop / PC)
1. เข้าสู่ระบบด้วยบัญชีที่มีสิทธิ์ `ADMIN` หรือ `SUPER_ADMIN`
2. ที่แถบเมนูด้านซ้าย (Sidebar) จะมีปุ่ม **"แผงควบคุมระบบ"** พร้อมไอคอนโล่สีน้ำเงิน
3. คลิกเพื่อเข้าสู่หน้า `http://localhost:3000/admin` ได้ทันที

### บนสมาร์ทโฟน (Mobile / Smartphone)
1. เข้าสู่ระบบผ่านเบราว์เซอร์บนมือถือ
2. แตะปุ่ม **"เพิ่มเติม (More)"** ที่แถบนำทางด้านล่าง (Bottom Navigation)
3. เลือกเมนู **"แผงควบคุมระบบ (Admin)"**

---

## 5. ฟังก์ชันการทำงานทั้ง 5 ส่วนหลัก

### 📊 แท็บ 1: ภาพรวมระบบ (Overview)
* **การ์ดสรุปตัวเลข:** แสดงจำนวนผู้ใช้งานทั้งหมด (แยกตามสิทธิ์), โน้ตทั้งหมด (แยกโน้ตทั่วไป และโน้ตในห้องนิรภัยที่เข้ารหัส), กระดานบอร์ด, และขนาดไฟล์ฐานข้อมูลจริงบนดิสก์
* **คำชี้แจงความปลอดภัย:** ยืนยันมาตรฐานการปกป้องความเป็นส่วนตัว ไม่มีการรั่วไหลของข้อมูลข้อความหรือรหัสผ่าน

### 👥 แท็บ 2: จัดการผู้ใช้งาน & สิทธิ์ (Users & Roles)
* **ค้นหาและกรอง:** ค้นหาผู้ใช้ด้วยอีเมล หรือ Username และเลือกกรองตามระดับสิทธิ์ได้
* **การเปลี่ยนสิทธิ์ (เฉพาะ `SUPER_ADMIN`):**
  - คลิกปุ่ม **"เปลี่ยนสิทธิ์"** ที่แถวของผู้ใช้ที่ต้องการ
  - เลือกระดับสิทธิ์ใหม่ (`USER`, `ADMIN`, `SUPER_ADMIN`)
  - กด **"บันทึกสิทธิ์"** ระบบจะอัปเดตและบันทึกลง Audit Log ทันที
  - *ระบบมีมาตรการป้องกัน: Super Admin ไม่สามารถลดสิทธิ์ตนเองได้ เพื่อป้องกันการล็อกตัวเองออกจากระบบ*
* **การลบผู้ใช้ถาวร (เฉพาะ `SUPER_ADMIN`):**
  - คลิกไอคอนถังขยะสีแดง **"ลบ"** ที่แถวของผู้ใช้ที่ต้องการลบ
  - กล่องยืนยันจะแสดงข้อมูลสรุปจำนวนโน้ต, บอร์ด และสมุดบันทึกที่จะถูกลบ
  - กด **"ยืนยันลบบัญชีนี้ถาวร"** เพื่อยืนยัน
  - *ระบบความปลอดภัย:* ห้ามลบบัญชีตนเอง, ห้ามลบ Super Admin คนสุดท้าย, และระบบจะลบข้อมูลที่เกี่ยวข้องแบบ Cascade Delete พร้อมบันทึกประวัติลง Audit Log ทันที

### 📋 แท็บ 3: กระดาน & สถิติโน้ต (Boards & 56 Notes Limit)
* **กฎควบคุมความจุกระดาน (Sticky Board 56 Rule):** แต่ละกระดานจำกัดโน้ตสูงสุดที่ **56 แผ่น** (`MAX_NOTES_PER_BOARD = 56`)
* **แถบวัดความจุ:** แสดงสถานะแบบเรียลไทม์:
  - 🟢 **ปกติ:** โน้ตน้อยกว่า 45 แผ่น (แถบสีน้ำเงิน/เขียว)
  - 🟡 **ใกล้เต็ม:** โน้ต 45 – 55 แผ่น (แถบสีส้ม)
  - 🔴 **เต็มแล้ว (FULL):** โน้ตครบ 56 แผ่น (แถบสีแดงกระพริบเตือน)

### 🗄️ แท็บ 4: ฐานข้อมูล & การสำรอง (Database & Backup)
* **สถานะความสมบูรณ์:** แสดงขนาดไฟล์ `dev.db`, วันที่แก้ไขล่าสุด, และผลลัพธ์จากคำสั่ง SQLite `PRAGMA integrity_check`
* **สั่งสำรองข้อมูลทันที (Create Snapshot Backup):**
  - พิมพ์หมายเหตุประกอบ (Optional Note) เช่น *"สำรองก่อนอัปเดตระบบ"*
  - กดปุ่ม **"กดสำรองข้อมูลเดี๋ยวนี้"** ระบบจะคัดลอกไฟล์ฐานข้อมูลไปยัง `backend/backups/` ทันที
  - ระบบจะเก็บประวัติสำรองสูงสุด **15 ชุดล่าสุด** และหมุนเวียนลบไฟล์เก่าอัตโนมัติ
* **ดาวน์โหลดไฟล์สำรอง (.db):**
  - คลิกไอคอนลูกศรชี้ลงที่ตาราง เพื่อดาวน์โหลดไฟล์ `.db` มาเก็บไว้ในเครื่องคอมพิวเตอร์ของคุณ (Offline Cold Backup)
* **กู้คืนฐานข้อมูล (Database Restore - เฉพาะ `SUPER_ADMIN`):**
  - คลิกปุ่ม **"กู้คืน"** ที่ไฟล์ที่ต้องการ
  - **ระบบความปลอดภัยหลายชั้น (Multi-layer Safety Modal):** บังคับให้พิมพ์คำว่า `CONFIRM_RESTORE` ลงในช่อง
  - **Auto Safety Backup:** ระบบจะสร้างสำรองของฐานข้อมูลปัจจุบัน (`pre_restore_safety_*.db`) ให้อัตโนมัติทันทีก่อนกู้คืนเสมอ เพื่อให้สามารถย้อนกลับได้หากต้องการ

### 📜 แท็บ 5: ประวัติกิจกรรม (Audit Logs)
* แสดงประวัติการทำงานของผู้ดูแลระบบอย่างละเอียด: วันเวลา, ชื่อแอดมิน, คำสั่งที่ทำ, เป้าหมาย, รายละเอียด และ IP Address
* **ส่งออกรายงาน (Export CSV):** กดปุ่ม "ส่งออก CSV" เพื่อดาวน์โหลดไฟล์รายงานบันทึกประวัติมาเปิดใช้งานใน Microsoft Excel

---

## 6. โครงสร้างไฟล์ในระบบแอดมิน

```text
NoteOnWeb/
├── ADMIN_GUIDE.md                         # เอกสารคู่มือเล่มนี้
├── backend/
│   ├── backups/                           # ที่จัดเก็บไฟล์สำรองฐานข้อมูล (.db)
│   ├── prisma/
│   │   ├── schema.prisma                  # โมเดล User (role), AuditLog, BackupRecord
│   │   └── dev.db                         # ฐานข้อมูล SQLite หลัก
│   └── src/
│       ├── controllers/
│       │   └── adminController.ts         # Logic หลังบ้านของ Admin ทั้งหมด
│       ├── middleware/
│       │   ├── auth.ts                    # ตรวจสอบ Token และโหลด Role
│       │   └── authorize.ts               # requireAdmin และ requireSuperAdmin
│       ├── routes/
│       │   └── adminRoutes.ts             # รวม Route /api/admin/* ทั้งหมด
│       └── server.ts                      # จุด Mount /api/admin
└── frontend/
    └── src/
        ├── services/
        │   └── adminService.ts            # Client API Service ฝั่งหน้าเว็บ
        ├── pages/
        │   └── admin.tsx                  # หน้า UI แผงควบคุมระบบ (5 แท็บ)
        └── components/layout/
            ├── Sidebar.tsx                # เมนูนำทางบน Desktop
            └── MobileBottomNav.tsx        # เมนูนำทางบน Mobile
```

---

## 7. คำถามที่พบบ่อย (FAQ & Troubleshooting)

**Q: ผู้ใช้ทั่วไปจะแอบเข้าหน้า Admin ได้หรือไม่?**  
A: ไม่ได้เด็ดขาด ระบบมีทั้ง **Frontend Route Guard** ที่ตรวจเช็คและเด้งกลับทันที พร้อมด้วย **Backend Middleware** ที่คืนค่า `403 Forbidden` หาก Token ไม่ใช่ `ADMIN` หรือ `SUPER_ADMIN`

**Q: หากเผลอกู้คืนฐานข้อมูลผิดไฟล์ จะแก้ปัญหาอย่างไร?**  
A: ไม่ต้องกังวล ทุกครั้งที่มีการสั่งกู้คืน ระบบจะแอบสร้างไฟล์ `pre_restore_safety_YYYY-MM-DD_HH-mm-ss.db` ของฐานข้อมูลเดิมเก็บไว้ให้อัตโนมัติในตาราง สามารถเลือกไฟล์ดังกล่าวเพื่อกู้คืนกลับมาได้ทันที

**Q: แอดมินสามารถดูรหัสผ่านของสมาชิกได้หรือไม่?**  
A: ไม่ได้ ระบบจัดเก็บรหัสผ่านด้วยการแฮชแบบทางเดียว (One-Way Hash ด้วย Bcrypt) และ API ของ Admin ถูกล็อกให้ตัดฟิลด์ความลับเหล่านี้ออกทั้งหมด 100%
